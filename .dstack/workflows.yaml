workflows:
  - name: train-mnist
    image_name: dstackai/python:3.9-cuda-11.6.0
    requirements: requirements.txt
    python_script: train.py
    artifacts:
      - data
    resources:
      gpu: ${{ gpu }}
