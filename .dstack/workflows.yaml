workflows:
  - name: train-mnist
    provider: python
    python: '3.9'
    requirements: requirements.txt
    python_script: train.py
    artifacts:
      - data
    resources:
      gpu: ${{ gpu }}
